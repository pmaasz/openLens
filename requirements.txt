# openlens Runtime Requirements
# Python 3.7+ required

# GUI Framework (Required)
PySide6>=6.2.0

# Scientific Computing & Visualization (Highly Recommended)
numpy>=1.19.0
matplotlib>=3.3.0

# Advanced Features (Recommended for full functionality)
scipy>=1.5.0
Pillow>=8.0.0

# Development tools (black, flake8, pytest, pre-commit, ...) are NOT
# listed here on purpose - install them via the extras group:
#   pip install -e .[dev]
